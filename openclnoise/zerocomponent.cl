PointColor /*id*/zerocomponent(PointColor input) {
    input.point./*id*/COMPONENT = 0;
    return input;
}

/*PointColor /*id*/zerocomponent(PointColor input) {
    input.color.x = input.point.x;
    input.color.y = input.point.y;
    input.color.z = input.point.z;
    input.color.w = 1;
    return input;
}*/

